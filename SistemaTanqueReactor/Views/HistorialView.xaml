<UserControl x:Class="SistemaTanqueReactor.Views.HistorialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <DockPanel>

        <!-- Filtros -->
        <Border DockPanel.Dock="Top" Style="{StaticResource ModernCard}" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Fecha:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <DatePicker Grid.Column="2" SelectedDate="{Binding FiltroFecha}" Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>

                <TextBlock Grid.Column="4" Text="Turno:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <ComboBox Grid.Column="6" 
                          ItemsSource="{Binding Turnos}"
                          SelectedItem="{Binding FiltroTurno}"
                          DisplayMemberPath="Nombre"
                          materialDesign:HintAssist.Hint="Todos"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"/>

                <StackPanel Grid.Column="8" Orientation="Horizontal">
                    <Button Style="{StaticResource PrimaryButton}" Command="{Binding BuscarCommand}" Content="Buscar" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource OutlineButton}" Command="{Binding LimpiarFiltrosCommand}" Content="Limpiar"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Resultado -->
        <TextBlock DockPanel.Dock="Top" Text="{Binding Mensaje}" 
                   Margin="0,0,0,12" Foreground="{StaticResource TextSecondaryBrush}"/>

        <!-- Tabla de historial -->
        <Border Style="{StaticResource ModernCard}" Padding="0">
            <DataGrid ItemsSource="{Binding Cargas}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      materialDesign:DataGridAssist.CellPadding="14,10"
                      materialDesign:DataGridAssist.ColumnHeaderPadding="14,12">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat='dd/MM/yyyy'}" Width="100"/>
                    <DataGridTextColumn Header="Turno" Binding="{Binding Turno.Nombre}" Width="110"/>
                    <DataGridTextColumn Header="N° Carga" Binding="{Binding NumeroCargaDelTurno}" Width="80"/>
                    <DataGridTextColumn Header="Operario" Binding="{Binding Operario.NombreCompleto}" Width="160"/>
                    <DataGridTextColumn Header="Tanque" Binding="{Binding NumeroTanque}" Width="70"/>
                    <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="100"/>
                    <DataGridTextColumn Header="pH" Binding="{Binding pH}" Width="60"/>
                    <DataGridTextColumn Header="Humedad" Binding="{Binding Humedad}" Width="80"/>
                    <DataGridTextColumn Header="Kg Elaborados" Binding="{Binding CantidadElaboradaKg, StringFormat='N0'}" Width="110"/>
                    <DataGridTextColumn Header="Lote Final" Binding="{Binding LoteProductoFinal}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

    </DockPanel>
</UserControl>
